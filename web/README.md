# Mango Hunt Web

React + Vite frontend for the Telegram WebApp shell.

## Run

```bash
npm install
npm run dev
```

The app includes:
- a Camera tab with live camera permission flow,
- a Found tab with My / All subtabs,
- a separate detail page for each found object.
